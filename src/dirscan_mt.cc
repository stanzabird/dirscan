#include "dirscan.h"

// Our own attempt at n-level generalization of the windirstat algorithm.
//
// First we get all the stuff in this folder.
// 
int dirscan_info::dirscan_mt(const std::string& dirname)
{
  return 0;
}

