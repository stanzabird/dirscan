#include "dirscan.h"

// Our own attempt of the windirstat algorithm.
//
// First we get all the stuff in this folder.
// 
int dirscan_info::dirscan_windirstat(const std::string& dirname)
{
  return 0;
}

